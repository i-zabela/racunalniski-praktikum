\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[slovene]{babel}

\usepackage{graphicx}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}

\usepackage{qtree}

\usepackage{hyperref}

\usepackage{color}
\definecolor{red}{rgb}{1,0.4,0.4}
\definecolor{green}{rgb}{0.4,0.75,0.4}
\definecolor{blue}{rgb}{0.4,0.4,0.75}
\definecolor{yellow}{rgb}{0.75,0.75,0.4}
\definecolor{grey}{rgb}{0.8,0.8,0.8}

% remove navigation symbols
\setbeamertemplate{navigation symbols}{}

% Try to use the Metropolis theme if it is installed; otherwise fall back to
% a standard beamer theme to avoid "Undefined control sequence"
% errors when Metropolis is not available on the system.
\IfFileExists{beamerthememetropolis.sty}{%
    \usetheme{metropolis}%
}{%
    % Metropolis not found — use a common built-in theme instead.
    \usetheme{Madrid}%
}

% map defaulf block to oldblock
\let\oldblock\block
\let\endoldblock\endblock

% change block by adding smallskip
\renewenvironment{block}[1]
    {\begin{oldblock}{#1}
        \smallskip
    }
    { 
    \end{oldblock}
    }

% map defaulf alertblock to oldalertblock
\let\oldalertblock\alertblock
\let\endoldalertblock\endalertblock

% change block by adding smallskip
\renewenvironment{alertblock}[1]
    {\begin{oldalertblock}{#1}
        \smallskip
    }
    { 
    \end{oldalertblock}
    }

% map defaulf exampleblock to oldexampleblock
\let\oldexampleblock\exampleblock
\let\endoldexampleblock\endexampleblock

% change block by adding smallskip
\renewenvironment{exampleblock}[1]
    {\begin{oldexampleblock}{#1}
        \smallskip
    }
    { 
    \end{oldexampleblock}
    }